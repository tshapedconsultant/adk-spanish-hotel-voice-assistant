[project]
name = "adk-spanish-hotel-voice-assistant"
version = "0.1.0"
description = "Spanish hotel reservation voice assistant (Gemini, Flask, optional Redis)"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Andres Lage" }]
requires-python = ">=3.11"
